"""Grammar files for the Pine compiler parser (D1 §1.5).

This package is an empty marker; the actual ``.lark`` files live alongside
this file. ``openbb_pine.compiler.parser`` loads them with
``lark.Lark.open(...)``.
"""
