"""The openbb-pine compiler — D1 territory.

This package houses the lexer (C1), parser/IR builder (C2), type checker (C3),
IR dataclasses (C4), codegen (C5), and compile cache (C6). Internals are
private; the public entry point is ``openbb_pine.pine_router``.
"""
